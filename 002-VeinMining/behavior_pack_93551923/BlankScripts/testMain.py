def SearchBlocks():
    offset = [-1, 0, 1]
    for y in offset:
        for x in offset:
            for z in offset:
                newPos = (x, y, z)


if __name__ == "__main__":
    SearchBlocks()
